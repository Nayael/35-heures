var Payslip = (function() {
    'use strict';

    /**
     * @constructor
     */
    function Payslip() {
        // enforces new
        if (!(this instanceof Payslip)) {
            return new Payslip();
        }
        
    }

    return Payslip;

})();